/**
 * Legacy migration CLI — skeleton (implemented in Phase 7, SPEC §10).
 * Usage (Phase 7): pnpm migrate:legacy --dump legacy/dump.sql --db <target>
 */

function main(): void {
  // eslint-disable-next-line no-console
  console.log('migrate-legacy: implemented in Phase 7 (see docs/SPEC.md §10).');
}

main();
